import { useMainPlayer } from "discord-player";
import { Interaction } from "discord.js";

export default async function pause(interaction: Interaction) {
    const {client, member} = interaction
    
}